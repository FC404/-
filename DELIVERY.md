# 工厂链 V1 交付说明

## 当前范围

V1 聚焦订单跟踪、物料库存、仓库出入库、客户资料、用户权限和移动扫码。加工厂接单模块已从当前版本主入口移除，保留为后续版本能力。

## 运行方式

开发调试：

```bash
npm run dev
```

本地生产试用：

```bash
npm run build
npm run start
```

启动后访问：

```text
http://127.0.0.1:8787/
```

默认登录：

```text
手机号：13800000001
密码：123456
```

正式试用服务会按用户表校验账号。

## 数据保存

前端会优先使用内置 API 保存数据；API 不可用时自动降级到浏览器本地存储。内置服务的数据文件保存在：

```text
server/data/
```

当前已支持服务端保存的业务数据：

- 订单
- 物料
- 客户
- 用户
- 仓库流水
- 操作审计

业务数据接口、备份、导入和恢复初始数据都需要登录令牌。页面登录成功后会自动携带令牌。

服务端首次启动会自动写入 V1 初始数据。如果数据文件被删除，重启服务或访问数据接口时会自动恢复初始数据。

## 备份与恢复

后台进入“系统维护”可以完成：

- 导出业务备份
- 导入业务备份
- 恢复初始数据

恢复或导入前，服务端会先在 `server/data/backups/` 自动保存一份当前数据备份。

## 交付前检查

```bash
npm run build
```

构建通过后再运行：

```bash
npm run start
```

检查页面、登录、订单新增、物料新增/编辑、物料导出、仓库流水新增和移动扫码页面。

还需要检查“系统维护”里的服务状态、备份导出和恢复初始数据。
